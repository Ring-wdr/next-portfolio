/**
 * Performance Benchmarks Index
 *
 * Export all benchmark utilities and configurations.
 */

// Re-export types
export type {
	InteractionConfig,
	PerformanceBudget,
	RouteConfig,
	ThrottlingPreset,
} from "./performance.config";
export {
	BENCHMARK_CONFIG,
	PERFORMANCE_BUDGETS,
	ROUTES,
	THROTTLING_PRESETS,
} from "./performance.config";
